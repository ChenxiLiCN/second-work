# HINSCAN 免物化因子索引算法设计

## 1. 结论先行

建议把第二项工作的第一篇算法论文聚焦在 **non-independent HINSCAN 的精确加速**，核心方案暂称 **FLI-HINSCAN（Factorized Lazy-Index HINSCAN，因子化懒惰索引 HINSCAN）**。

它不构造 HINSCAN 的元路径转换图 `G_P`，而是把 `G_P` 看成一次布尔连接的结果，只保存连接两侧的“半元路径—中心见证”关系。查询时从该因子化表示中按需生成候选元路径邻居，先用安全上界剪枝，再对少量边进行精确结构相似度验证。已经验证的相似度被写入稀疏证书缓存，后续不同 `epsilon`、`mu` 查询直接复用。

这条路线同时满足四个目标：

1. **免物化**：不保存 `G_P` 的全部点对边；
2. **精确**：主算法与 NIHINScan/NIHINScan+ 返回完全一致的 cluster、core、non-core、hub 和 outlier；
3. **支持重复调参**：离线因子索引和在线相似度证书可跨 `epsilon`、`mu` 复用；
4. **可扩展到动态图**：只增量维护半元路径关系，不显式更新可能二次爆炸的 `G_P` 边集。

不建议第一阶段同时处理 independent HINSCAN。独立路径验证本身已经是另一条复杂主线，会稀释“免物化索引”的核心贡献。可以在主方案完成后，将独立模型作为扩展或下一篇工作。

## 2. 现有方法真正的瓶颈

给定 HIN `G`、对称元路径 `P`、相似度阈值 `epsilon` 和核心阈值 `mu`，HINSCAN 先构造元路径转换图：

`G_P = (V_A, E_P)`，其中 `(u,v) in E_P` 当且仅当 `u` 与 `v` 之间至少存在一条 `P` 的路径实例。

之后在 `G_P` 上计算：

`sigma_P(u,v) = |N_P[u] intersect N_P[v]| / sqrt(d_P(u)d_P(v))`。

HINSCAN+ 虽然选择顶点数最少的类型作为 pivot，并从 pivot 向元路径两侧搜索，但仍会把两侧结果做笛卡尔积并写入 `G_P`。若某个 pivot `x` 对应 `r_x` 个目标顶点，仅该 pivot 就可能诱导 `Theta(r_x^2)` 条点对边。

因此真正的瓶颈不是“沿元路径搜索一次有多快”，而是：

> 是否必须把一个高度可因子化的连接结果展开为全部点对边，才能运行 HINSCAN？

答案是否定的。

## 3. 核心表示：把元路径图写成因子化布尔乘积

### 3.1 半元路径见证

先考虑实验中最常用的对称元路径：

`P = A_0 ... A_h ... A_0`。

令 `P_L` 为从目标类型 `A_0` 到中心类型 `A_h` 的左半元路径。对目标顶点 `u`，定义：

`W_P(u) = {x | u 能沿 P_L 到达中心见证 x}`。

对中心见证 `x`，定义倒排表：

`L_P(x) = {u | x in W_P(u)}`。

则有精确等价关系：

`v in N_P(u)  <=>  存在 x in W_P(u)，使得 v in L_P(x)`，

即：

`N_P[u] = {u} union union_{x in W_P(u)} L_P(x)`。

若用布尔矩阵 `B` 表示目标顶点到中心见证的半元路径可达性，则转换图邻接矩阵只是概念上的：

`A_P = sign(B B^T)`，结构邻域还需按 HINSCAN 定义显式包含顶点自身。

FLI-HINSCAN 只保存 `B` 的双向压缩表示，而不计算和保存 `B B^T`。

### 3.2 为什么它可能大幅节省空间

令：

`F = sum_x |L_P(x)|`，表示半元路径因子关系的规模。

显式物化可能产生：

`m_P <= sum_x C(|L_P(x)|, 2)` 条边。

当少数中心顶点连接大量目标顶点时，`F` 是线性的，而 `m_P` 可能是二次的。该场景正是 HINSCAN 转换阶段最慢、同时也是因子化收益最大的场景。

必须如实说明：最坏情况下因子关系本身也可能很大，而且精确全图检测仍可能需要检查大量候选边。本方案的优势是避免不必要的全量展开，并使代价与实际访问的因子表和验证边数量相关，而不是声称在所有图上都突破最坏情况界。

## 4. 离线索引结构

### 4.1 必需部分

对每条固定元路径 `P` 建立：

1. `W[u]`：目标顶点 `u` 可达的中心见证列表；
2. `L[x]`：中心见证 `x` 对应的目标顶点倒排表，使用排序数组或 Roaring Bitmap；
3. `d[u]`：`|N_P[u]|` 的精确值；
4. `degBand[u]`：按度数区间组织目标顶点，供度比剪枝使用；
5. `blockUB[u]`：将目标顶点 ID 空间划分为 `b` 个块后，记录 `N_P[u]` 在各块中的数量或安全上界；
6. `version`：为后续增量维护预留的顶点和见证版本号。

`d[u]` 的计算只需要临时合并 `W[u]` 对应的倒排表并做基数统计，结果只保存一个整数，不保存完整 `N_P[u]`。

### 4.2 可选部分

1. `KMV[u]` 或 Bottom-K sketch：估计两个结构邻域的交集，用于安排验证顺序；
2. `Cert[u]`：稀疏相似度证书表，保存已经精确验证过的 `(v, intersection, sigma)`；
3. `CTCert[u,mu]`：当已经证明某个 `mu` 下第 `mu` 大相似度阈值时，保存局部 core-threshold 证书；
4. 热点顶点的临时 `N_P[u]` bitmap 缓存，采用 LRU/内存预算控制。

在“精确模式”中，sketch 只能决定先检查谁，不能直接决定接收或拒绝一条边；最终判断必须来自安全上界或精确交集计数。在“近似模式”中，才允许用置信区间直接分类，并给出类似 sandwich guarantee 的结果界。

## 5. 索引构建算法

### 5.1 自适应分割点，而不是只看类型顶点数

HINSCAN+ 选择元路径中顶点数最少的类型作为 pivot，但顶点数少不等于连接代价低。更合理的代价应同时考虑：

- 左右半路径可达关系的非零元数量；
- pivot 倒排表的长度分布；
- 预期查询时的 posting 扫描量；
- 中间关系是否会快速变稠密。

对元路径每个合法分割位置 `j`，通过轻量采样估计：

`Cost(j) = nnz(B_left_j) + nnz(B_right_j) + lambda * ExpectedPostingWork(j)`。

选择代价最低的分割位置。对严格对称元路径，中心位置通常最自然；对一般首尾同类型元路径，可保存左右两套因子表：`L_j(x)` 与 `R_j(x)`。

### 5.2 构建步骤

```text
BuildFactorIndex(G, P):
    1. 采样评估各候选分割位置，选择 pivot position j
    2. 只计算 P 的前缀/后缀可达关系，生成 W[u] 与 L[x]/R[x]
    3. 压缩并排序各倒排表
    4. 对每个 u 临时合并相关倒排表，得到精确 d[u]
    5. 构建度数分桶与 block upper-bound 摘要
    6. 可选：从倒排表合并 Bottom-K sketch
    7. 初始化空的 Cert 与 core-threshold 证书
```

与 HINSCAN+ 的关键差异是：第 2 步结束后不执行 `L[x] x R[x]` 的笛卡尔积。

## 6. 在线精确查询

### 6.1 候选元路径邻居按需生成

对顶点 `u`，用 `W[u]` 指向的多个倒排表做去重 k 路归并，生成且仅生成 `N_P(u)`。生成器是惰性的：下游算法不再需要候选时立即停止。

### 6.2 三层相似度判断

对候选边 `(u,v)`，按以下顺序处理。

#### 第一层：度比安全剪枝

因为交集不超过较小邻域：

`sigma_P(u,v) <= sqrt(min(d[u],d[v]) / max(d[u],d[v]))`。

若该上界小于 `epsilon`，可在 `O(1)` 时间精确拒绝。

等价地，若：

`d[u] < epsilon^2 d[v]` 或 `d[v] < epsilon^2 d[u]`，则一定不相似。

#### 第二层：分块交集上界

设 `h_u[t]` 是 `N_P[u]` 在第 `t` 个 ID 块中的数量或安全上界，则：

`|N_P[u] intersect N_P[v]| <= sum_t min(h_u[t], h_v[t])`。

若右端小于：

`T(u,v) = ceil(epsilon * sqrt(d[u]d[v]))`，

则精确拒绝。该剪枝不依赖随机性。

#### 第三层：按块精确验证并提前终止

只对剩余边，按需构造或读取 `u`、`v` 的邻域块 bitmap，逐块执行 AND + popcount。

- 当前交集数达到 `T(u,v)`：立即接收；
- 当前交集数加上未扫描块的最大可能贡献仍小于 `T(u,v)`：立即拒绝；
- 否则继续下一块。

最终交集计数和相似度写入 `Cert[u]` 与 `Cert[v]`，避免重复计算。

### 6.3 与 pSCAN 的 core-first 框架结合

对每个顶点维护：

- `sd(u)`：已确认相似邻居数量，下界；
- `ed(u)`：仍可能相似的邻居数量，上界。

查询时优先处理 `ed` 最大的顶点：

- `sd(u) >= mu`：确认 core；
- `ed(u) < mu`：确认 non-core；
- 否则继续从惰性候选生成器取边并验证。

确认 core 后，继续枚举其全部 `epsilon`-邻居，并用并查集连接相似的 core-core 边。全部 core 处理结束后，再把与 core 相似的 non-core 顶点加入相应 cluster，最后按照其连接的 cluster 数量区分 hub 与 outlier。

```text
FLI-HINSCAN-Query(Index, epsilon, mu):
    初始化 sd、ed、并查集和边证书视图
    按 ed 非增序处理顶点 u:
        若已有证书足以判断 u，直接复用
        否则惰性生成 u 的 P-neighbor 候选 v:
            若 Cert(u,v) 有效，复用结果
            否则依次执行度比剪枝、分块上界、精确块交集
            更新 u、v 的 sd/ed
            一旦 sd(u) >= mu 或 ed(u) < mu，停止 core 判定阶段
        若 u 为 core，补全其 epsilon-neighbor，并连接 core-core 边
    由并查集生成核心簇
    将相似 non-core 加入簇，并识别 hub/outlier
```

## 7. 半离线、半增量证书索引

完整 GS*-Index/SC-Index 会预先计算并排序所有邻接点对的相似度，这在 HIN 中会重新引入 `G_P` 物化。这里改成“证书驱动”的懒惰索引：

1. 离线阶段只建因子关系、度数和摘要；
2. 第一次 `(epsilon,mu)` 查询只验证为回答该查询所必需的点对；
3. 已验证点对保存精确相似度，因此可服务任意后续 `epsilon`；
4. 对每个 `u`，当已验证的第 `mu` 大相似度不小于所有未验证候选的安全上界时，才固化 `CTCert[u,mu]`；
5. 后续查询可像 GS*-Index 一样直接依据该证书判定 core，但不要求全局 neighbor-order 已经完成。

这个设计在查询序列上具有“逐步学习”特性：常用区域和常用阈值会越来越快，冷区域永远不支付完整物化成本。

## 8. 动态图扩展

### 8.1 维护半路径支持数

删除底层边时，同一 `(u,x)` 半路径关系可能仍由其他路径实例支持。因此不能只保存布尔值，需要维护：

`cnt(u,x) = u 到 x 的半元路径实例数`。

仅当 `cnt(u,x)` 在 `0` 与正数之间转换时，才插入或删除因子关系。

底层边更新 `delta e` 的影响可通过“更新边左侧前缀可达集合 × 右侧后缀可达集合”计算，只更新受影响的 `(u,x)` 支持数，而不生成受影响的全部目标点对。

### 8.2 懒惰失效与重算

当 `L[x]` 变化时，所有引用 `x` 的顶点邻域都有可能变化。若立即逐个更新，热门 pivot 会造成大范围传播。因此采用 heavy-light 策略：

- `|L[x]| <= tau` 的轻 pivot：立即标记相关顶点缓存和证书失效；
- `|L[x]| > tau` 的重 pivot：只增加 `version[x]`，顶点在下次查询访问时检查版本并重算；
- 多次更新积累到批阈值后，对重 pivot 做一次批量重建。

相似度证书保存其依赖的邻域版本。版本不一致的证书绝不用于最终判断，从而保持精确性。

### 8.3 更新复杂度目标

令 `Delta F` 为一次底层更新导致的半路径因子关系变化数，`Delta E_P` 为若显式物化时发生变化的元路径图边数。目标更新代价为：

`O(prefix/suffix delta join + Delta F + light invalidation)`，

而不是 `Theta(Delta E_P)`。在热门中心见证场景中，`Delta E_P` 可能远大于 `Delta F`。

## 9. 正确性证明框架

论文中至少给出以下结果。

1. **因子化邻域等价引理**：惰性 posting 合并生成的集合恰好是 `N_P(u)`；
2. **候选完备性引理**：所有且仅有元路径邻居被生成，无漏边和伪边；
3. **度比剪枝安全性引理**；
4. **分块上界安全性引理**；
5. **提前终止正确性引理**：接受或拒绝时均已达到不可逆阈值；
6. **查询等价定理**：FLI-HINSCAN 与 non-independent HINSCAN 返回完全相同的所有顶点角色；
7. **增量维护定理**：支持数和版本机制保证任意更新序列后使用的因子表、度数与证书均与当前 HIN 一致。

## 10. 复杂度表达

设：

- `n_A`：目标类型顶点数；
- `F`：半元路径因子关系数；
- `m_P`：物化转换图边数；
- `b`：摘要块数；
- `K`：可选 sketch 大小；
- `Qscan`：一次查询实际扫描的 posting 元素数；
- `X`：实际进行精确交集验证的候选边集合。

索引空间可写为：

`O(F + b*n_A + K*n_A + |Cert|)`，

其中 `|Cert|` 随真实查询逐步增长，而不是预先等于 `m_P`。

查询时间可表达为：

`O(Qscan + sum_{(u,v) in X} VerifyCost(u,v) + alpha(n_A)*UnionOps)`。

该表达比简单声称 `O(m_P^1.5)` 更能体现算法优势：只为访问的因子表、未被剪枝的边和真实输出簇付费。

## 11. 与直接相关工作的边界

| 方法 | 是否物化同构图/相似图 | 是否精确 HINSCAN | 是否支持多参数复用 | 是否支持动态图 |
|---|---:|---:|---:|---:|
| HINSCAN / HINSCAN+ | 是 | 是 | 否 | 否 |
| SCAR | 否 | 否，目标为近似 Louvain | 部分 | 否 |
| GS*-Index | 是，保存完整邻接及相似度序 | 是，同构 SCAN | 是 | 是 |
| SC-Index | 是，保存相似图和核心阈值集 | 不是 HINSCAN 定义 | 是 | 未解决元路径边爆炸 |
| 动态 StrClu | 基于给定同构图 | 近似边标签 | 固定参数 | 是 |
| FLI-HINSCAN | 否，保存半路径因子 | 是 | 是，懒惰证书复用 | 可扩展 |

最需要强调的创新不是“用了 bitmap/sketch”，而是：

> 第一次把 HINSCAN 的元路径转换图视为可因子化的布尔连接结果，并在不展开点对关系的前提下完成精确结构聚类；进一步把完整 neighbor-order 改造成查询驱动的稀疏证书索引。

## 12. 实验设计

### 12.1 数据集与元路径

优先复用 HINSCAN 的 TMDB、IMDB、DBLP、Foursquare、DBpedia，并补充 Amazon 或 Freebase。对称元路径长度取 2、4、6，尤其要覆盖热门 pivot 导致高 fan-out 的路径。

### 12.2 基线

1. NIHINScan；
2. NIHINScan+；
3. 显式 `G_P` + pSCAN；
4. 显式 `G_P` + GS*-Index，用于多参数查询对比；
5. 因子索引但关闭所有剪枝；
6. 若实现近似模式，再与 SCAR/LSH 风格近似筛选比较，但需说明目标函数不同。

### 12.3 指标

- 索引构建时间与峰值内存；
- 单次查询时间；
- 多组 `(epsilon,mu)` 查询总时间及 break-even 查询次数；
- `m_P/F` 物化膨胀比；
- 扫描 posting 数、生成候选数、度比剪枝率、块上界剪枝率；
- 精确相似度验证数、提前终止比例、证书命中率；
- 更新延迟、失效证书数、批量重建频率；
- 精确模式与 HINSCAN 输出逐点一致率，必须为 100%。

### 12.4 消融顺序

`Factor-only -> + degree-ratio -> + block upper bound -> + lazy bitmap cache -> + certificate reuse -> + sketch scheduling -> + incremental maintenance`。

### 12.5 应主动报告的失败场景

- `m_P` 本身很稀疏时，因子层和缓存管理可能不如 HINSCAN+；
- 大部分候选度数相近且邻域高度重叠时，度比与块上界剪枝较弱；
- 单次查询如果几乎所有顶点都是 core，最终仍可能接近展开全部相关边；
- 很长元路径可能先导致半路径因子 `F` 爆炸，需要前缀复用或限制路径长度。

## 13. 最小可行原型

第一版只做：

1. non-independent HINSCAN；
2. 对称长度 2 的 `A-B-A`，随后扩展到长度 4；
3. `W[u] + L[x]` Roaring Bitmap 因子索引；
4. 精确 `d[u]`、度比剪枝、按需 bitmap OR/AND；
5. pSCAN 的 `sd/ed` 和并查集；
6. 与 HINSCAN+ 做输出一致性、时间和内存对比。

只有当该原型在高 fan-out 数据上显示明显优势，再加入分块摘要、懒惰证书和增量维护。这样可以快速验证核心假设，避免一开始堆叠过多组件。

## 14. 两句话论文定位

现有 HINSCAN 必须显式构造由元路径诱导的同构图，而高 fan-out 中心顶点会把线性的半路径关系膨胀为二次规模的点对边，使转换阶段成为主要时间和内存瓶颈。

我们以半元路径见证倒排表因子化表示该同构图，并结合惰性候选生成、安全相似度上界、按需精确验证和可复用证书，在不物化元路径图的情况下返回与 HINSCAN 完全相同的结构聚类结果。
